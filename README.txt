To use the API, add "/images" and a query string 
containing the filename (with the extension)
and the height and width you want to resize the image.

The test scripts are empty, and I ignored some eslint rules.